;; sqrt(s*(s-a)*(s-b)*(s-c)), s=(a+b+c)/2  -- triangle area (Heron); cancels badly for thin triangles

(let a (Var "a"))
(let b (Var "b"))
(let c (Var "c"))

(let s (Div (Add (Add a b) c) (Num 2)))

(let start
  (Sqrt
    (Mul
      (Mul s (Sub s a))
      (Mul (Sub s b) (Sub s c)))))
