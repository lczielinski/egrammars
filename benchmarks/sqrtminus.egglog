;; sqrt(x*x + 1) - x
;; = 1 / (sqrt(x*x + 1) + x)
;; = ((x*x + 1) - x*x) / (sqrt(x*x + 1) + x)
;; = x * (sqrt(1 + 1/(x*x)) - 1)
;; = (1/x) / (sqrt(1 + (1/x)*(1/x)) + 1)

(let x (Var "x"))

(let start
  (Sub 
    (Sqrt (Add (Mul x x) (Num 1)))
    x ))