[project]
name = "egrammar"
version = "0.1.0"
description = "Compile an e-graph of equivalent programs into a CFG for constrained sampling"
requires-python = ">=3.12"
dependencies = [
    "casa",
    "egglog==13.2.0",
]

[tool.uv.sources]
casa = { path = "../casa", editable = true }
